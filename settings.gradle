pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
        jcenter()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS) // Or PREFER_PROJECT if necessary
    repositories {
        google()
        mavenCentral()
        jcenter()
    }
}
rootProject.name = "MyMath"
include ':app'
